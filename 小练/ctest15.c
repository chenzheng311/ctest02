#include <stdio.h>
int main(void)
{
    int x = 1, y = 1, z, i;
    scanf("%z", &z);
    if (z = 1)
    {
        printf("z=%d\n", z = 1);
    }
    else if (z = 2)
    {
        printf("z=%d\n", z = 2);
    }
    else if (z >= 3)
    {
        for (i = 1; i < +20; i++)
        {
            z = x + y;
            x = z;
            y = x;
            printf("%d\n", z);
        }
    }

    return 0;
}